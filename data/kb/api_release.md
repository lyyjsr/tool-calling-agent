# API 发布检查清单

## 发布前必查项
1. 本地测试全部通过。
2. 新增接口在 /docs 中可见。
3. 请求和响应 schema 已同步更新。
4. 新增环境变量已写入 .env.example。
5. 数据库变更已验证初始化不会报错。
6. 关键路径已手工回归。

## 风险检查
- 是否修改了核心路由
- 是否新增数据库表或字段
- 是否影响已有会话数据
- 是否可能导致任务查询失败

## 发布建议
- 优先选择低峰期发布。
- 如果涉及数据结构变更，先在本地验证初始化逻辑。
- 发布后优先检查 /health、/chat、/tasks 两类核心接口。